module.exports = {
  conf: {
    aliases: [''],
    name: '',
    help: '',
    enabled: true,
    slash: false, // Prefix ile kullanılacak
  },

  run: async ({ client, message, args }) => {
  }
};
